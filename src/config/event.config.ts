export enum ServiceEvents {
  Created = 'service.created',
  Started = 'service.started',
  Paid = 'service.paid',
}

export enum CardEvents {
  Created = 'card.created',
  Empty = 'empty.cards',
}

export enum PaymentEvents {
  SoftDelete = 'soft.delete.payment.method',
}
